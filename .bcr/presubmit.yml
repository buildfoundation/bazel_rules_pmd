matrix:
  platform: [ "macos", "ubuntu2004" ]

tasks:
  verify_targets:
    name: "Verify build targets"
    platform: ${{ platform }}
    build_targets:
      - "//pmd/wrapper:bin"
    test_targets:
      - "//tests/analysis:tests"
