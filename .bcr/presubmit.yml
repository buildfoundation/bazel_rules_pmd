matrix:
  platform: [ "macos", "ubuntu2004" ]

tasks:
  verify_targets:
    name: "Verify build targets"
    platform: ${{ platform }}
    test_flags:
      - "--enable_bzlmod=true"
    test_targets:
      - "@rules_pmd//..."
